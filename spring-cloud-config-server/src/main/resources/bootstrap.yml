server:
  port: 8888
  
spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: https://github.com/jihao/spring-cloud-config-demo
          clone-on-start: true
          search-paths:
            - config-repo